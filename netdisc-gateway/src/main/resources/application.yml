server:
  port: 8070

spring:
  application:
    name: netdisc-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 101.43.95.238:9070
    gateway:
      globalcors: #解决跨域问题
        corsConfigurations:
          '[/**]':
            allowedHeaders: "*"
            allowedOriginPatterns: "*"
            allowCredentials: true
            allowedMethods: "*"

      routes:
        - id: netdisc-filesystem
          uri: lb://netdisc-filesystem
          predicates:
            - Path=/api/files/**,/api/folders/**,/api/stores/**,/api/search/**
          filters: #排除/api
            - StripPrefix=1
            - AddResponseHeader=X-Response-Default-Foo, Default-Bar

        - id: netdisc-user
          uri: lb://netdisc-user
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=1
            - AddResponseHeader=X-Response-Default-Foo, Default-Bar





